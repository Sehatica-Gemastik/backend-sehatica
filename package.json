{
  "name": "backend-sehatica",
  "license": "MIT",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed-arms": "bun run src/scripts/seed-arms.ts",
    "db:seed-portal": "bun run src/scripts/seed-portal-demo.ts",
    "db:migrate-portal": "bun run src/scripts/migrate-portal.ts",
    "db:clear-questionnaire-logs": "bun run src/scripts/clear-questionnaire-logs.ts"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.0",
    "postgres": "^3.4.9",
    "unpdf": "^1.8.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  }
}